import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Item = ({id, title, width, height, url, download_url}) => {
  return (
    <View style={{}}>
      <Text>{title}</Text>
    </View>
  );
};

export default Item;

const styles = StyleSheet.create({});

{
  /*rnfes */
}
